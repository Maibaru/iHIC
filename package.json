{
  "name": "halal-inventory-checker",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "generate": "node generate-pages.js",
    "build": "npm run generate"
  },
  "dependencies": {
    "csv-parser": "^3.0.0"
  }
}